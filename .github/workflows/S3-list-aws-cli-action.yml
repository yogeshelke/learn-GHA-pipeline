name: List S3 Contents
on:
  push
jobs:
  listS3:
    runs-on: ubuntu-latest
    steps:
      #- uses: chrislennon/action-aws-cli@v1.1
      # - name: install-aws-cli-action
      #   uses: unfor19/install-aws-cli-action@v1.0.2
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:sts::143568017883:assumed-role/AWSReservedSSO_AWSAdministratorAccess_dd77ed1972b1c2d8/yogesh.shelke@fielmann.com
          aws-region: eu-central-1
      - name: List S3 Buckets in AWS CLI
      run: |
        aws s3 sync . s3://my-s3-test-website-bucket
      - run: aws s3api list-buckets
       # −env:
       #  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
       #  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
